def ingest(data_path):
    print(f"ingest: {data_path}")
    # Convert source data into a standardised format for algorithm consumption.
    # Delete raw input data, move standardised input data into folder named after it, check data README
