def validate(data_path):
    print(f"validate: {data_path}")
    # compare each standardised alg result to the standardised input data
    # alg results should be in data folder, under data_path name
